databaseChangeLog:
  - changeSet:
      id: 5
      author: antigravity
      comment: "Add user_id FK to delivery_cost_config for per-user delivery rate overrides (Fallback Model)"
      changes:
        - sqlFile:
            path: classpath:db/changelog/v1.2.0/sql/01-add-user-delivery-rates.sql
            splitStatements: true
            stripComments: true
